{
  "name": "cr-wasm-tools",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build-wasm": "wasm-pack build ./src/wasm --target web",
    "build": "wasm-pack build ./src/wasm --target web && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^1.0.14",
    "@types/chrome": "^0.0.270",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  },
  "dependencies": {
    "vanjs-core": "^1.5.2"
  }
}
